PORT=4000
JWT_SECRET=devsecret
FRONTEND_ORIGIN=http://localhost:5173
# DATABASE_URL provided by Render in production

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@dhstore.rw

FORCE_HTTPS=false

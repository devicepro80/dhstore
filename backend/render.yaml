services:
  - type: web
    name: dhstore-backend
    env: node
    plan: free
    buildCommand: "npm install && npx prisma generate"
    startCommand: "node index.js"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dhstore-db
          property: connectionString
      - key: JWT_SECRET
        value: changeme123   # ⚠️ Replace with a strong secret
      - key: FRONTEND_ORIGIN
        value: http://localhost:5173   # or your frontend deployed URL later
      - key: SMTP_HOST
        value: smtp.example.com        # change to your email provider
      - key: SMTP_PORT
        value: 587
      - key: SMTP_USER
        value: your@email.com
      - key: SMTP_PASS
        value: yourpassword
      - key: SMTP_FROM
        value: no-reply@dhstore.rw
      - key: FORCE_HTTPS
        value: false

databases:
  - name: dhstore-db
    plan: free

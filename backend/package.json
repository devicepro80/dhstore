{
  "name": "dhstore-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
  "dev": "node --watch src/index.js",
  "build": "mkdir -p dist && cp -r src dist && cp -r prisma dist/prisma",
  "seed": "node prisma/seed.js",
  "postinstall": "npx prisma generate || true",
  "start": "node dist/index.js"
},
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "argon2": "^0.31.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "nodemailer": "^6.9.10",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  }
}
